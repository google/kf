# Test:	TestGenTroubleshooterRunbook/cluster_with_problems
# friendlyname: ClusterFoo
# gvr:
#   Group: kf.dev
#   Resource: clusterfoos
#   Version: v1alpha1
# namespaced?: false
# version: v1.0

{# This page is autogenerated by Kf. File a bug under componentid:590084 rather than changing it directly. #}
{% extends "migrate/kf/docs/v1.0/_base.html" %}
{% block custom_heading %}
{% block page_title %} Troubleshoot {{kf_product_name_short}} ClusterFoos {% endblock %}
<meta name="description" content="Resolve issues with {{kf_product_name_short}} ClusterFoos">
<meta name="gtm_var" data-key="docType" data-value="troubleshooting">
{% endblock %}
{% block body %}

Use these steps to troubleshoot various issues that can occur when using {{kf_product_name_short}} ClusterFoos.

## First problem. {:#problem-0}

Run the following command to get the resource information, then check for the causes listed below:

```posix-terminal
kubectl get clusterfoos.kf.dev {{ '<var>' }}CLUSTERFOO_NAME{{ '</var>' }} -o yaml
```

The `kf` CLI can help check for some of the issues:

```posix-terminal
kf doctor clusterfoo/{{ '<var>' }}CLUSTERFOO_NAME{{ '</var>' }}
```

<table>
<thead>
<tr><th>Possible Cause</th><th>Solution</th></tr>
</thead>
<tbody>
<tr>
<td>
First Cause.
</td>
<td>
<p><strong>Some</strong> recommendation <a href="https://example.com">link</a>.</p>

</td>
</tr>
<tr>
<td>
Second Cause.
</td>
<td>
<p><strong>Some</strong> recommendation <a href="https://example.com">link</a>.</p>

</td>
</tr>
</tbody>
</table>

## Second problem. {:#problem-1}

Run the following command to get the resource information, then check for the causes listed below:

```posix-terminal
kubectl get clusterfoos.kf.dev {{ '<var>' }}CLUSTERFOO_NAME{{ '</var>' }} -o yaml
```

The `kf` CLI can help check for some of the issues:

```posix-terminal
kf doctor clusterfoo/{{ '<var>' }}CLUSTERFOO_NAME{{ '</var>' }}
```

<table>
<thead>
<tr><th>Possible Cause</th><th>Solution</th></tr>
</thead>
<tbody>
<tr>
<td>
Cause 2.1.
</td>
<td>
<p><strong>Some</strong> recommendation <a href="https://example.com">link</a>.</p>

</td>
</tr>
<tr>
<td>
Cause 2.2.
</td>
<td>
<p><strong>Some</strong> recommendation <a href="https://example.com">link</a>.</p>

</td>
</tr>
</tbody>
</table>

{% endblock %}

