package: servicebrokers
common:
- name: Namespace
  type: string
  description: the Kubernetes namespace to use.
  default: '"default"'
configs:
- name: Create
  options:
  - name: Params
    type: 'map[string]interface{}'
    description: service-specific configuration parameters.
  - name: Url
    type: 'string'
    description: broker's endpoint URL.
- name: Get
- name: Delete
- name: List